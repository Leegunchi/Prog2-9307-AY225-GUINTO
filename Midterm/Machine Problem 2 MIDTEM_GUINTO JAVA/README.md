# My machine problem 2!

hello sir as you can see, I have three versions of the machine problem 2 (technically only two versions but the first version is to show you the errors I made on the paper.)

first version is the one I wrote in paper and is the incorrect version in which I accidently wrote a code that was using python syntax instead of java

the second version is the least efficient version which works with any scale of matrix and also uses recursion (even though the instructions doesn't tell me to use it. )

the third version is the most efficient version of my code (due to me not using recursions anymore and just sticked to using for loop) I only made the second one because I wanted to make a version that works no matter how many collumns or rows you put in.